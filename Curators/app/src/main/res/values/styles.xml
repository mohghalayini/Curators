<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/concordiared</item>
        <item name="colorPrimaryDark">@color/concordialessred</item>
        <item name="colorAccent">#ffff</item>
    </style>

    <style name="ToolbarStyle" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:colorBackground">@color/concordiared</item>
        <item  name="android:buttonTint">#ffff</item>
        <item name="android:textColor">#ffff</item>
    </style>

</resources>
